# messaging_app/kubeservice.yaml
apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app
    version: blue   # switch to 'green' when ready
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
